---
name: create-follow-up
description: Create a tracked Work Overview follow-up item for an explicitly selected matter.
---

# Create follow-up

Use `create_follow_up_item` only for a material item selected by the client or user. Supply a
concise neutral title and summary. Attach source references separately with
`attach_evidence_to_follow_up`.
